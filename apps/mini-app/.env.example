# Relative path — Vite's dev proxy forwards /api -> http://127.0.0.1:8081,
# and the same relative path keeps working behind any reverse proxy or tunnel.
VITE_API_BASE_URL=/api/v1
VITE_BOT_USERNAME=
VITE_FONT_PROVIDER_URL=https://fonts.googleapis.com
VITE_APP_VERSION=0.1.0
